# Rehab Monitor backend dependencies (MVP)
# Application-layer deps stay pinned for reproducibility across macOS dev /
# Windows deployment. The numerics/ML stack uses lower bounds (per the
# implementation plan, Phase 2 Task 4) so a developer can pull in any
# compatible torch/numpy wheel from the system package manager without
# fighting strict equality pins.

Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
channels==4.1.0
daphne==4.1.2

# Numerical / determinism utilities used by the analyzer layer.
numpy>=1.21.0

# CTR-GCN analyzer runtime. CPU wheels are enough for live inference on a
# single-patient sliding window; swap to a CUDA wheel for GPU deployments.
torch>=1.10.0

# Testing runs through Django's built-in test runner — no extra deps needed.
